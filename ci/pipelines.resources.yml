resources:
  - name: fileSpecsJavaSnapshotGit
    type: GitRepo
    configuration:
      # TODO: Need to change to jfrog after the repo is created
      path: asafgabai/file-specs-java
      gitProvider: asaf_github
      buildOn:
        pullRequestCreate: true
      branches:
        # TODO: Need to change to main or master
        include: new-project-for-file-specs
      cancelPendingRunsOn:
        pullRequestUpdate: true

  - name: fileSpecsJavaReleaseGit
    type: GitRepo
    configuration:
      # TODO: Need to change to jfrog after the repo is created
      path: asafgabai/file-specs-java
      gitProvider: asaf_github
      buildOn:
        commit: false